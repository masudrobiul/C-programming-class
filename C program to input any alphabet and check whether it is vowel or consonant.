#include <stdio.h>
int main ()
{
    //Write a C program to input any alphabet and check whether it is vowel or consonant.

    char ch;
    scanf ("%c", &ch);

    if(ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u' || ch == 'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U')
    {
        printf ("The letter is vowel", ch);
    }
    else
    {
        printf ("The letter is consonant", ch);
    }

    return 0;
}
